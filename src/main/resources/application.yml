spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  application:
    name: blogProject
  h2:
    console:
      enabled: true
  profiles:
    include: oauth, jwt